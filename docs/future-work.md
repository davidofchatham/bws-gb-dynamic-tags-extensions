# Future work tracker

**Not a roadmap for future work.** No committed timeline on anything in the future
tables. One exception: **In-flight** rows name a target version, but even those hold
no progress detail — the row points at the branch / plan / unreleased CHANGELOG,
which own the real build state. The tracker never tracks phase, commit, or
percent-done (that surface drifts — see the FW-52 staleness that motivated this
rule). This is a single visible index of non-bug work — future AND in-flight.
Each row has a stable **`FW-N` id**, a short **item** title,
a **description**, its hard **blockers** (what must land first, typed), its
**interactions** (what it reshapes or is reshaped by, by id), and a pointer to
wherever the detail actually lives. It duplicates no detail — open the linked home
for the full design/rationale. Cross-refs use ids, never prose, so a reworded row
never orphans a reference.

- **Bugs do NOT go here** → GitHub Issues (`bug` label).
- **Enhancement detail** lives in its home (a `.claude/plans/*.md` plan file, a
  GitHub `enhancement` issue, or a memory note) — this table only tracks *that it
  exists, what gates it, what it touches, and where to read more*. The detail home
  also carries **certainty** (a `Concept` plan vs a locked one) — that's why there is
  no per-row status column.
- **Lifecycle is the SECTION, not a column.** Two axes, don't conflate: *certainty*
  (concept → planned) is read from the detail home; *lifecycle* (future → in-flight →
  shipped) is read from which section the row sits in. A row starts in a future
  section, **moves to `### In flight`** when committed build work begins (branch +
  work toward a named version), and moves to **Closed / Retired** on ship. The row
  keeps its `FW-N` and its pointer cells through all three — only its section changes.
  This coarse move is the ONLY progress signal the tracker carries; no phase/commit/
  remaining-tasks ever enters a cell (that drifts — the FW-52 lesson).
- Some homes are local/hidden (`.claude/plans/` is gitignored, memory files sit
  outside the working dir). This tracker is the tracked, reviewable surface over
  them. Migrate detail into `docs/` opportunistically; until then the link still
  points home.

## Column meaning

| Column | Holds |
|---|---|
| **ID** | Stable `FW-N` handle. Cross-refs use this, never prose. IDs are permanent — a shipped/cut row's ID is retired, never reused. |
| **Item** | Short title. |
| **Description** | The detail + rationale hooks. |
| **Blocked by** | Hard prerequisite, typed: `row:FW-N` (another row) · `ship:X.Y.Z` (a version — satisfied once shipped) · `decision:<what>` (an open choice) · `code:<condition>` (a code state). `—` = unblocked. |
| **Interacts with** | Softer coupling (reshapes / reshaped-by / ship-near) as `FW-N` ids + external `#issue`. Not a gate. |
| **Target** | *In-flight table only.* The version the in-flight work is landing in (e.g. `1.17.0`). A soft timeline, not a gate; the branch / CHANGELOG own the actual state. Future/possibility rows have no Target column. |
| **Detail home** | Where the design/rationale + implicit certainty (concept vs planned) live. |

> **Agent pickup:** a FUTURE-table row is startable when `Blocked by` is `—` or every `row:`/`ship:` gate it names is satisfied. `decision:`/`code:` gates are human-resolved — don't auto-start those. `Interacts with` never blocks. An **In-flight** row is already being built — do NOT pick it up as new work; read its detail home for real state before touching it.

## Trackers

### In flight

Committed build work toward a named version. **Pointer-only, like every other row** — the branch /
plan / unreleased CHANGELOG own the real build state; this table names only that the work is live,
what it touches, where to read it, and its target version. No phase/commit/remaining-tasks here. A row
lands here from a future section when build starts and leaves for Closed / Retired on ship.

| ID | Item | Description | Blocked by | Interacts with | Target | Detail home |
|---|---|---|---|---|---|---|
| FW-51 | try_ slot 2+ silently empty without `use` | A `try_` slot 2+ configured with only `N-key:` renders EMPTY — no warning, no preview hint; it reads as broken fallthrough (fallthrough is fine: `2-use:title` resolves). `N-src:same` does not rescue it. Tripped over twice in one day (2026-07-21, incl. by an agent), so the discovery cost is real and recurring. Not a runtime bug — a controls/UX gap: the editor should either default `use` for slots 2+, require it visibly, or warn in the tag configuration preview. Take with the controls/options rework rather than standalone. **FW-57's slot fold CLOSES this by construction (assessed 2026-08-03): the broken shape becomes UNEXPRESSIBLE** — a folded slot's field picker renders only when the Read kind is `key`, so an author cannot set a key without setting the read. Mechanism confirmed: `class-tag-template-registry.php:701-703` discards `key_raw` when `use` is empty under `per_slot_use`, so `$has_new` is false and the slot is skipped. Legacy wire migrates faithfully (the skip is preserved); only the AFFORDANCE that creates it goes away. ⇒ verify + retire alongside FW-57 rather than building a warning. | — | **FW-57 (closes)**, FW-20, FW-13, FW-17 | 1.17.0 | `.claude/plans/combined-option-controls.md`; closure rationale `.claude/plans/src-chain-encoding.md` §The FLAG surface; contract row `tools/test/src-site-test-matrix.md` R4.6 |
| FW-53 | `{{table}}` repeater→HTML table tag | Repeater fold (`rows` step) → `<table>` string in a text-as-div block. Prototype built + committed. **Reframed 2026-07-26 (the src chain decides how many rows):** a table's row-set = whatever its src chain returns (returns N → N rows, returns 1 → 1-row), NOT a flat/repeater/relationship "mode"; repeater becomes an author-facing src-chain STEP token (parallel to `srcTermIn`). This makes the table authoring surface HARD-DEPEND on the multi-step src-selection encoding model (FW-56) — v1 finalization is gated there. The cell reader (row-kind-branched `meta_row`/`post`/`term`) is producer-agnostic and stays table-owned. Relationship rows = a `ref` fan-out chain (rides FW-32, not a table mode); a post-query row-source is FW-54. Remaining breakdown: detail home Roadmap — NOT duplicated here. **⚠ THE BLOCKER CLEARS WITH GH #55** (base-tag source chains — arm dispatch + chain authoring + migration, specced 2026-08-05): that IS the FW-56 authoring surface this row waits on, so table v1 resumes the moment it lands, and its §Roadmap tiers want re-reading against the shipped chain rather than the prototype's. **AND THE RELEASE WAITS ON TABLE, not on #55:** `bws_register_table_tag()` is called unconditionally at plugin load, so a 1.17.0 that ships today exposes the prototype. Either finish table v1 or gate the registration before release — do not let the tag ship half-built by default. | `row:FW-56` (authoring surface — clears with GH #55) | FW-13, FW-14 (#12 discovery scoping), FW-20, FW-32, FW-54, FW-56 | 1.17.0 | `.claude/plans/table-tag.md` §Roadmap |
| FW-56 | Multi-step src-selection encoding + authoring model | How a MULTI-STEP source chain (`src` → `refs` → `terms` → a promoted repeater `entries` step → …) SERIALIZES on the flat `}`-free GB option wire AND PRESENTS as ONE coherent source selector in the editor. The engine has always chained internally (`bws_run_traversal`); no authoring surface exposes an arbitrary ordered chain. **Reach is every tag whose source is not ambient — base tags included, not only slots:** FW-57's fold is slots-only, and slots inherit chains rather than the reverse. **Wire + COMPILE shipped 1.17.0** (grammar `slot-fold.php`, compiler `slot-fold-compile.php`, vocabulary rows in `deprecated-tags-options.md`). What REMAINS: the authoring surface, and the base depth-0 migration — **both landing 1.17.0**, sequenced after the verb-agnostic resolver refactor (FW-63: dispatch by the chain's RESOLVED-SOURCE KIND), which container arms still block by comparing flat `src`/`srcTermIn` tokens. Binding constraint on anything built here: a slot picker must compute the chain's resolved-source kind + type STATICALLY from the wire, with no live probe and no chain run (I8) — which forbids a lossy or opaque step encoding. Decisions and open questions both live in the detail home; read its §SETTLED index before re-deriving any of them. | — | FW-32, FW-24, FW-20, FW-53, FW-39, FW-13, FW-52, FW-57, FW-59, **FW-63** | 1.17.0 | `.claude/plans/src-chain-encoding.md` — §SETTLED index (every closed decision, with the section that owns it) + the OPEN table beside it. **SUB-PLAN `.claude/plans/per-hop-limit.md`** owns per-hop `limit` semantics/defaults, the compile-time legacy-cap rule, and base-tag chain authoring-vs-migration; it WINS on every `limit` question and carries its own §SETTLED index |
| FW-57 | Slot-payload fold — read-step + slot repeater | Fold a slot's whole `src-chain → field-READ` into ONE slot value — the read is a sibling bracket-kv token beside `src(…)`, naming the chain's LAST step — plus a slot REPEATER (add/remove) replacing the `show_if_any` reveal chain that made slot N+1 appear only once slot N held a value. SLOTS-ONLY by decision: base tags keep `src:` + `use:`/`key:`. Distinct from FW-56 (the chain wire itself) and FW-24 (whole-tag-in-slot); this is the tractable middle. **BUILT in 1.17.0** (unreleased): grammar owner + JS twin, the `bws-slot-fold` control, BOTH migration paths (the `post_content` converter and tag-modal mount, which reach different content), `{{join}}` and all nine `try_` tags flipped, capital slot keys (`A:`, `B:`, …) with format tokens following them, chain compile, matrices + visible fixtures. **Closes FW-51 by construction** — a key with no read becomes unexpressible. Held OUT of scope, not overlooked: per-slot `sep` and the five datetime keys under the fold, per-slot LINK (→ FW-20), multi-option atomic tags (→ FW-24). Decisions and what is still open both live in the detail home's §SETTLED index and OPEN table. | — | FW-56, FW-24, FW-20, FW-45, FW-52, FW-53, FW-51, FW-39, FW-60 | 1.17.0 | `.claude/plans/src-chain-encoding.md` — §SETTLED index (every closed decision, with the section that owns it) + the OPEN table beside it |
| FW-63 | Verb-agnostic arm dispatch — base callbacks branch on the chain's RESOLVED-SOURCE KIND, not on flat `src`/`srcTermIn` tokens | ≈19 render-path sites across five files still pick a resolution ARM by comparing `$options['src']` to `'ref'`/`'site'` or by reading `$options['srcTermIn']` directly. A base tag carrying CHAIN wire therefore renders a **different value, not an empty one** — measured: `{{text src:terms,department\|use:title\|limit:0}}` prints the PAGE title where `srcTermIn:department` prints `Sales, Support`, and `src:refs,x` silently loses list mode because `$is_ref` is false. **This is the only real gate on base-tag chain authoring**, which has no other encoding for multi-step relationships. Smaller than it looks: the arms conflate two axes the compiler has ALREADY separated, so the fix is two chain queries — `bws_fold_src_root_token()` (**exists**; `field-helpers.php:548` is the shipped precedent, since `src:site;entries,rows` roots at site but resolves to a meta_row) plus a NEW resolved-source-kind query (`post\|term\|meta_row\|site`, and whether it FANS). NB kind is a property of the CHAIN, not of its last step — a root-only chain (`src:site`) has one and has no steps at all — then ~19 mechanical swaps. **The DELIVERABLE is matrix coverage, not the swaps:** every base family needs a flat-vs-chain equivalence row per arm on the testbed, because a wrong arm renders a plausible value rather than nothing. Long-anticipated as the "verb-agnostic resolver refactor" (`{{join}}`'s own-loop was to be its second instance); filed and scoped in 2026-08-05. **It also HARD-GATES base depth-0 MIGRATION** (registered, same release): migrating flat→chain puts every stored base tag through chain arms at once, on pages nobody opened, so a broken arm goes from affecting hand-converted tags to affecting the whole corpus on upgrade. Arms + their matrix coverage must be COMPLETE before the migration entry registers. | — | FW-56 (the gated consumer), FW-32, FW-53, FW-57, FW-26 | 1.17.0 | `.claude/plans/per-hop-limit.md` §Arm dispatch, sized |

### Correctness, Consistency, Architecture

| ID | Item | Description | Blocked by | Interacts with | Detail home |
|---|---|---|---|---|---|
| FW-3 | Route datetime through the L1/L2 seam | **Half (a) SHIPPED 1.15.0** (term-ambient parity + resolved-source rethread): the id-arg param-overload contradiction is retired — `bws_parse_combined_date_time` and the four datetime cores take the factory's resolved-source payload and branch on kind; legacy scalars (bare int / `'option'` / `"{tax}_{id}"`) coerce through `bws_datetime_coerce_read_target()` (datetime-helpers.php), the compat shim bws-portal-system + try_/term_ closures pin — FW-3 residue, dies with FW-38 migration. Bare datetime tags on a term archive now read the term's date field (matrix D7). **Remaining = half (b)**: full seam routing through `bws_resolve_field_values` — gated: datetime reads value + field-object formats per key, seam return is frozen `string[]` → needs the seam return-shape design FW-49 owns (datetime = second consumer of that shape, not owner of its own arm). Gate re-pointed 2026-07-20 (rescan): the earlier "content/image/analog arms" framing was phantom — see FW-4 outcome. **Dependency direction partly inverts (2026-07-21 rescan):** datetime is structurally AHEAD of text/title here — `bws_datetime_collect_list()` is already the extracted, datetime-agnostic loop FW-49's convergence target wants, and datetime already suppresses the per-item fallback deliberately. So FW-49 borrows datetime's shape rather than supplying one; what datetime still needs from FW-49 is the per-value `{value,id,kind}` payload replacing `first: mixed` (which forces the `instanceof WP_Term` / `is_numeric` sniff at all four call sites), plus the field-object formats. **Payload half LANDED 1.16.0 (FW-49 build):** the four datetime call sites ride the shared `bws_collect_value_list()` fold — the `instanceof WP_Term`/`is_numeric` first-item sniff is gone, replaced by the fold's per-value link identity; `bws_datetime_collect_list()` deleted. **Remaining residue:** datetime VALUE reads still go through the cores (`bws_read_field`/`get_field`), not `bws_resolve_field_values` — full seam routing needs the field-object-formats read the seam does not expose. | `decision:field-object formats through the seam` | FW-43, FW-35 | `.claude/plans/fw3-datetime-seam.md` (half (a) shipped record + half (b) framing); payload half's record `.claude/plans/archive/traversal-convergence-fw49.md` |
| FW-7 | Collapse `bws_read_field`'s internal loop/term-archive resolution | field-helpers.php:271-296 → into the source factory. Surfaced during Phase 1 (T4/V12): the seam already bypasses it via explicit id; once the wrapper routes all ~30 callers through the factory, that inference duplicates the factory everywhere. NOT Phase 1 (touches the 30-caller read path). | `code:wrapper routes all callers through factory` | FW-8 | `.claude/plans/traversal-pipeline.md` §Post-Phase-1 convergence |
| FW-8 | Fold `bws_reliable_term_context_detection` into `bws_capture_ambient_signals` | Two term-detection impls coexist (5-tier in taxonomy-helpers + factory signals; surfaced Phase 1); factory is the intended home. NOT Phase 1 (used by `TaxonomyTerm::resolve_id` + `term_` modifiers → widens blast radius mid-refactor). | `row:FW-7` | FW-33 | `.claude/plans/traversal-pipeline.md` §Post-Phase-1 convergence |
| FW-9 | Context-aware base tags across remaining WP contexts | date / search / PTA / 404 / latest-home — the query-context (entity-less) kinds (Q5/Q7). Term kind SHIPPED 1.14.0 (Phase 1); AUTHOR kind SHIPPED 1.15.0 (`title`/`content` only — see FW-47 for its permalink/image residue). Each remaining kind carries a query/date/search payload with no field to read and needs its own option surface. | `decision:per-kind option surface` | FW-33, FW-47 | GH #19 + `.claude/plans/context-aware-base-tags.md` |
| FW-10 | `src:site` → ref | Resolve an entity from a site-stored relational field. | — | FW-32 | GH #28 |
| FW-38 | Explicit `registered_by` + `lifecycle` entry fields (retire the callback proxy) | Path Y follow-on to the FW-36 split (shipped 1.14.0) / its §B1 correction. Today tag box-placement leans on callback-presence as a de-facto "internal-removed vs external-still-registered" marker, with a `prefix_removed` override bolted on (interim; `MigrationRegistry::is_entry_live()` PHPDoc + CONTEXT.md I10). Principled replacement: record `registered_by` (internal vs external plugin id) and `lifecycle` (`unset=active` \| `deprecated` \| `removed`) at `register()` time; box placement reads `lifecycle` only, callback becomes irrelevant to classification, internal-removed entries carry an explicit `removed` marker. Feeds portal-system coordination (external declares its own `registered_by` + `lifecycle`; handoff in `bws-portal-system/.claude/plans/prefix-removed-handoff.md`). NOT this release. | — | — | memory `project_registered_by_lifecycle.md`; CONTEXT.md I10 (interim state it replaces) |
| FW-43 | Verb-agnostic slot-resolver extraction | Parameterized fold (selecting=first-wins \| combining=collect-all) + reveal-shape over the shared slot machinery. try_'s resolve loop + `show_if_any` reveal remain inline/selecting-shaped in `generate_base_try_tags()` (#26 extracted only option DEFINITIONS); `{{join}}` (1.15.0) is the second instance — its own collect-all loop + combining reveal in `bws_join_callback()`/`bws_get_join_options()` now shape the extraction target. Third instance: the datetime list collect loop (`bws_datetime_collect_list()`, 1.15.0 #30) — already extracted and datetime-agnostic in shape (slice/skip/join/first), a natural seed for the shared fold. **Fourth instance, moved here from FW-49 (2026-07-21 rescan): the base CONTENT callback's srcTermIn loop** (base-tags.php:1094-1104) — genuine first-wins-and-`return`-out-of-callback, no limit/sep, no ref-plural branch, no link identity; it is a SELECTING fold, so it belongs to this row's parameterized fold, not FW-49's combining one. Note its `$post_id` is computed unconditionally (:1092, no `$is_ref` guard, since there is no fanning-ref branch to double-step against), so under `src:ref` it silently collapses to the first ref target via `bws_base_post_id_from_source` — decide on convergence whether that collapse is intended or a latent gap. Also the natural home for un-hardcoding the `same`-use prepend (join couldn't take `per_slot_use` from the shared emit because of it). **Absorbs FW-5** (merged 2026-07-20): the `try_core_fn`/`try_term_fn`(/`try_site_fn` post-FW-4) fork collapse to ONE kind-dispatching handler lands as an internal step of this extraction — the fold's slot handler IS the collapse target; doing them separately double-touches the densest callback (V8 byte-identity risk). The fork's missing USER leg is now an observable gap: on an author archive try_text renders empty while {{text}}/{{join}} resolve (1.16.0 text user arm rode the ABSORB seam; try_ slots read through the descriptor table instead) — the collapse inherits the user arm for free. The "seam expansion proves the kind-dispatch shape" gate FW-5 carried was already satisfied (`bws_read_resolved_source`'s kind switch is that shape, shipped 1.14.0). FW-4's wiring (shipped 1.15.0) landed the three-legged fn table (`try_site_fn`) the collapse generalizes over. | — | FW-49 | `.claude/plans/archive/combine-text.md` §Build locality; memory `project_open_refactors.md` |
| FW-47 | Author-kind permalink + image analogs | The 1.15.0 author kind shipped `title`/`content` only; `{{permalink}}`/`{{image}}` on an author archive render empty (honest gap) pending two design calls. **permalink** — datum EXISTS (`bws_resolve_link_url` already resolves `get_author_posts_url`), so shipping is trivial; the open question is whether it's WORTH it. On the author archive itself a bare `{{permalink}}` IS circular (the URL of the page you're on) — and the non-circular uses don't reach this path: a multi-author loop resolves each row as a POST (loop-row wins over user-ambient, so it's post permalink there), and `src:ref`→user isn't wired. So the author permalink analog only earns its keep once a NON-ambient user source exists (`src:author` FW-48, `src:ref`→user, or the ID source FW-39 pointing at a user) — better gated on FW-48/FW-32/FW-39 than decided standalone. **image** — no clean intrinsic analog (parity with the #29 term-image gap): the avatar (`get_avatar_url`) is the candidate but adds an external Gravatar HTTP + privacy surface and isn't "featured-image" semantics; a `use:key` ACF user-image field already works (key-mode, not analog). Decide per tag; datetime author analog folds in with FW-9's datetime work, not here. | `decision:image-avatar-analog` (permalink soft-gated on a non-ambient user source, FW-48/FW-32/FW-39) | FW-9, FW-48, FW-32, FW-39 | `.claude/plans/context-aware-base-tags.md` §Tag Dispatch (author rows) + `bws_base_user_analog_read` PHPDoc (the two deferred cases) |

### Feature follow-ups & UX

| ID | Item | Description | Blocked by | Interacts with | Detail home |
|---|---|---|---|---|---|
| FW-13 | Smart field selector v2/v3 | **v1 SHIPPED 1.13.0** (discovery-backed `bws-field-combo` replacing GB `key`/`ref`/`linkKey`/datetime-key text inputs; ACF + sub-fields + options-page + term-meta + registered meta; flat searchable list, two filters [location path + type], kind/group-dynamic label, free-text + clear; REST `bws-dynamic-tags/v1/fields` inlined per editor load, registration-time not value-time; offered⟺resolvable). **Remaining:** v2 type-priority (recommend-divider OR multi-select Filter 2; `ref`→relationship+post_object; `src:ref` stepped-to-PT scope; smarter Filter-1 preset; dynamic label on `ref`; custom combobox widget for reopen-highlight); v3 Pie Calendar; v-future pick-a-post-to-scan. | — | FW-14, FW-20 | `.claude/plans/field-selector.md` |
| FW-14 | Field-selector post-v1 follow-ups | Ultra review 2026-07-06: **FU-1** location filter carries kind/group as structured data, not a parsed display string (fixes `' › '` delimiter forgery + locale-fragile kind inference; G2 parenthetical-strip already band-aided); **FU-2** `bws_field_key_option()` factory for the ~14 hand-copied option flips (fold into v2); **FU-3** shared filter set for the datetime_ key controls (4 stacked filter pairs on `datetime_range` → one tag-level set; needs a cross-control state channel since GB renders each option control independently). All low-severity. | — | FW-13, FW-41 (pair-combine halves FU-3 stacking), FW-53 ({{table}} #12 lands FU-1 prior art + FU-3 2nd instance + FU-2 prop) | `.claude/plans/field-selector.md` §Post-v1 follow-ups |
| FW-15 | `{{phone}}` follow-ups | display format, ext/type affix, per-country rules, `use` enum, per-tag `cc:`, lenient passthrough, vanity/spelled display. | — | — | `.claude/plans/phone-tag-future.md` |
| FW-16 | `{{call}}` v2 ergonomics cluster | pretty `$meta['label']` in select/mirror; `post_id_arg` registration-repoint (post_id-aware-but-not-first fns); multi-arg `args:` single-control (CSV/typed serialization, comma-in-value escaping); `arg:` enum-constraint (`$meta` whitelist); allowlist shape B/C (DB allowlist gated by the filter; A extends non-breakingly); shortcode-replacement ambition. All non-breaking — v1 storage is already associative. | — | FW-24 (multi-arg CSV shares tech) | `.claude/plans/archive/fn-passthrough-tag.md` §Deferred |
| FW-17 | Src-dynamic use-entry labels (V10a) | Relabel select `options[]` by active source. | — | FW-18 | GH #33 |
| FW-18 | Per-value `show_if` gating for select `options[]` | | — | FW-17 | GH #27 |
| FW-19 | Base-tag distinguishing suffixes | e.g. "Text (cross-source)". | — | — | Under consideration |
| FW-55 | Warn + escape UI for tag-string-unsafe chars in free-text options | The separator/format free-text options (`{{join}}` `valueSep`/`sep`/`format`, datetime `format`, any future glue/subvalue delimiter) let an author type a value that silently corrupts on editor reopen: a **2nd** `:` in a pair loses its tail to the JS KV-split's limit-2 discard, and `{`/`}` fail the render matcher outright (constraint verified GB 2.3.0 — `gb-constraints.md` §Separator-safe vs unsafe characters + §Tag string escape syntax). `bws-format-input` already escapes `\:`/`\|` for the datetime `format` case (v1.7.4+); this row generalizes that: (a) a help-text note on each free-text separator/format option naming the safe set + the `\:`/`\|` escape + the no-`{}` rule, and (b) extend the escape-on-save/unescape-on-display control coverage to the join glue options so a typed `:` round-trips instead of truncating. Not a runtime bug — an authoring-UX gap; take with the controls/options rework, not standalone. | — | FW-20, FW-44, FW-15, FW-16 | `docs/gb-constraints.md` §Separator-safe vs unsafe characters (the constraint); `.claude/plans/combined-option-controls.md` (controls rework home) |
| FW-58 | Title tag does not suppress GP's native content title | A DTE title tag inside a GP Page Hero / Page Header renders a **duplicate** — the native title stays on. GP self-suppresses only for its own literal `{{post_title}}`: both the legacy Page Hero (`class-hero.php:889`) and the Page Header module (`page-header/functions/functions.php:32`) `strpos()` the raw stored element content at `wp` and add `generate_show_title → __return_false`. DTE never touches that filter (verified 2026-07-29, GP 3.6.1 / Premium 2.5.6) and its tag syntax is not that literal, so neither path fires. Inverse of the GP toggle case: GP's own tag self-suppresses, DTE's cannot. Two levers — (a) `add_filter('generate_show_title','__return_false',20)`: no filter exists on the suppression *decision* (the `strpos` result feeds `add_filter` directly, unhookable), but all writers sit at priority 10 and 20 is GP's own override slot (`plugin-compat.php:845`); must register no earlier than `wp`:101 to land after Page Hero's `wp`:100. (b) `generate_page_hero_post_title` (`class-hero.php:947`) rewrites the *substituted value*, buying correct suppression at the cost of routing through GP's tag. Open call: whether suppression is opt-in per tag — unconditional would break authors who deliberately want both, and DTE cannot know from the tag alone whether it sits inside a hero. **Scope widened 2026-07-29 (sitewide `{{title}}`):** GP has TWO title signals, so suppression must cover both or it half-works. GP's own handler does them together (`class-hero.php:888-895`) with an **asymmetric guard** — `generate_show_title` gated on `is_singular()`, `generate_archive_title` + `get_the_archive_title` ungated, plus a `post_class` hentry strip. Mirror that shape: gating both alike strips archive loop-item titles on singular or leaves a duplicate heading on archives. **Common case needs no work:** a Page Hero *Block* Element's "Disable title" checkbox already performs both removals with correct guards, so `{{title}}` inside that hero is clean today; this row is only about use OUTSIDE such an element (content-template, hook element, ordinary block). Condition-side pairing (a page-type-dispatching "Title Active" rule) is GLC's T15/T16, not DTE's — the seam needs no API between the plugins: GLC answers "did the author disable the title here", DTE answers "what does `{{title}}` resolve to", and they compose in the editor. | `decision:opt-in vs unconditional suppression` | FW-9 (remaining archive contexts) | `bws-generate-layout-conditions/docs/architecture.md` §Content title: complete writer survey (rows 5/6 + sister-plugin interaction) |

### Testing & infrastructure

| ID | Item | Description | Blocked by | Interacts with | Detail home |
|---|---|---|---|---|---|
| FW-42 | Fixture testbed (seeded WP site + render seam) | Dedicated browsable fixture site on the wp-litespeed env; blueprint-composing seed (`tools/fixtures/<blueprint>/` = manifest.php data contract + schema.php + seed.php; env `bin/seed.sh` runner) so sibling plugins (portal-system, Meta Conductor) compose shared infrastructure while owning their fixtures; `wp bws render-tag --url` seam (spike PROVEN 2026-07-16, `tools/debug/spike-render-seam.php`) makes discovery rows + manual matrix rows one CLI call. Deliverable A = seed the two existing matrices' state; B accretes per-tag manifests (spec work). | — | FW-3 (datetime term-ambient row is FW-3(a)'s pre-written acceptance test), FW-13 (editor surface eyeball env) | `.claude/plans/fixture-testbed.md` |

### Future possibilities

| ID | Item | Description | Blocked by | Interacts with | Detail home |
|---|---|---|---|---|---|
| FW-20 | Combined option controls | `use:key,field` serialization combine (selector+field fold) — SEPARATE from FW-13 (this is the wire change, that is discovery). Serialization-ORDER half carved out to FW-52 (shipped 1.16.0); the `link` cluster unification stays here. | — | FW-13, FW-40, FW-41 | `.claude/plans/combined-option-controls.md` (srcTermIn part shipped v1.6.0) |
| FW-21 | Add sources to GB core tags via JS filters | | — | — | `.claude/plans/gb-tag-extension.md` |
| FW-23 | Base text tag: treat `'0'` as empty (opt-in) | Augments hooks.php:37 preserve-zero — absorbed by `{{join}}` slots for free (yields the athletics `5'` height suppression). | — | — | `tag-reference.md` §join "'0' is a real value" (context); `.claude/plans/archive/combine-text.md` §Empty-value detection |
| FW-24 | Tag-in-slot composition | Slots hold whole base tags → heterogeneous join/try. NB the nested-braces syntax can never ride the wire (GB kills any `}` in options — `gb-constraints.md` §Tag-string-unsafe values); encoding must stay flat. Candidate flat-but-structured encoding NOTED (no lean): bracket grouping `2:src[…],use[…]` — `[`/`]` are wire-safe and give named, order-free sub-fields (dodges the serialization-order fragility of positional CSV), at the cost of a hand-rolled two-sided sub-parser + bracket-balancing/escape (brackets are visual only — GB gives no parser scope, so a `:`/`\|`/`}` inside a group is still live; `gb-constraints.md` §Separator-safe vs unsafe characters). This is the "pairs" answer to the row's open positional-vs-pairs question. **The 2026-07-29 sandbox VALIDATED this direction** — the bracket-kv slot schema (`name(value)`, slot bracket as kv delimiter) IS named bracket sub-fields, and settled the escape half FW-24 flagged: brackets inert to GB + balance-aware sub-parser (WE own it) → balanced inner brackets survive, `\:`/`\|` escape the live GB chars, `{`/`}` remain hard-unsafe (this row's nested-braces bar). See FW-56/57 §2026-07-29. **MOSTLY SATISFIED by the approved FW-57 fold wire (user assessment 2026-07-31 — an assessment, NOT a build).** The fold's slot value already carries a whole per-slot source chain + its read, and Option R has the read NAME ITS PROCESSING TAG on format-agnostic containers (join / table columns) — which IS heterogeneous-tag-per-slot, arriving for free, flat, with no nested braces. So this row's three original asks are answered: (a) flat-but-structured encoding = bracket-kv; (b) positional-vs-pairs = pairs (named tokens, order-free); (c) escape discipline = settled above. **⚠ PREREQUISITE LANDED IN FW-56/57 (2026-07-31): the step splitter must be BRACKET-AWARE, and it must be built that way FIRST.** Verified empirically: a naive `preg_split('/[,;]/')` over a step segment shreds any token carrying nested or free-form content — `refs,x,tag[datetime_single,format[g:i]]` (the FW-24 whole-tag-in-step shape) splits mid-token into `["refs","x","tag[datetime_single","format[g:i]]"]`. The per-step `limit[5]` token decided 2026-07-31 survives the naive splitter only by ACCIDENT (a bare integer cannot contain a separator), so the hazard is invisible until FW-24 arrives. Splitter must track bracket depth and split only at depth 0, mirroring the token-level depth guard at `proto-fold-tag.php:146-157`. This IS the "balance-aware sub-parser (WE own it)" this row already names — the new fact is that writing it naively now means FW-24 replaces it and every wire stored in between is ambiguous. Corollary: bracket ALTERNATION (L1 `()` / L2 `[]`) stops being cosmetic — alternating pairs are what make depth trackable through nesting, so the alternation rule and nested-bracket safety are one mechanism. **Remaining FW-24 scope is now narrow: per-type OPTION tokens inside a slot** — a slot naming `datetime_single` cannot yet carry that tag's `format`, an image slot cannot carry `as`/`size`, etc. (FW-57 explicitly defers multi-option atomic tags here.) That residue is what would still need a real design pass; the composition question itself is largely closed. Re-title this row when the fold builds. | — | FW-25, FW-16, FW-56, FW-57 | memory `deferred_features.md` (north-star for #26); sandbox → `.claude/plans/src-chain-encoding.md` §2026-07-29 |
| FW-25 | Multislot-only field options | Gate a `use` value to slot ≥2. | — | FW-24 (cheaper alt) | memory `deferred_features.md` |
| FW-26 | `{{if}}` conditional tag | THIRD composition verb (selecting=try / combining=join / conditional=if): branch a template/value on a READ field value. Driver: athletics `bws_get_game_result` (term-name → template). NOT a join requirement. **PARKED in favor of FW-27 (user, 2026-08-01)** — the `if` concept has settled as an embedded per-slot option on base tags, not a separate tag set. Keep this row for the tag-shaped alternative and the athletics driver; read FW-27 first. | — | FW-27, FW-28 | memory `deferred_features.md` (loose concept, no plan) |
| FW-27 | `if:` as a BASE-TAG OPTION | Lighter alt to FW-26 — `srcTermIn`-style trigger + `show_if` predicate grammar server-side: self-gate one tag's output, OR adjacency-exclusion. Propagates: try inherits per-tag free, join per-slot (rides the same `{N}-` slot encoding join/try_ use now; an IF'd-out join slot should likely collapse like an empty one). No-ELSE = parity w/ GB Conditions + Block Visibility. **DIRECTION CONFIRMED + SHARPENED (user, 2026-08-01): embedded option, NOT a separate tag set** — this row is the live one, FW-26 the parked alternative, gated only on building a UI that stays out of the way until wanted. **Relationship to `try_` settled: `if` COMPOSES with the slot chain, it does not replace it.** `try_` is functionally an if-HAS-VALUE chain — a slot chain whose per-slot predicate is hardcoded to has-value — so generalizing the predicate makes `if` a per-slot CONDITION on that same chain (evaluate slot: guard passes AND read non-empty → return; else advance). Two predicates per slot, not one: the has-value check on the slot's own read (today's behavior, the default) plus an optional author-set guard. Under FW-60 this collapses BOTH proposed tag families into one option surface on base tags. **The expensive part, and the open decision: the condition's SUBJECT.** User confirms the useful cases test a DIFFERENT source/field than the slot reads (check `is_featured`, output `title`) — so a condition needs its own source + field pair, i.e. its own src-chain per slot, on top of the read chain the slot already carries. That roughly doubles per-slot state and returns the UI wall FW-57's fold exists to remove; user accepts both control and serialization complexity are real here. Cheap fallback if it bites: force same-subject (condition tests the slot's own read), which covers has-value + simple truthiness and needs no second chain — but sacrifices the cases that motivate the feature. Decide subject-coupling BEFORE any wire work; it sets the whole cost. Wire rides FW-56/FW-57 (a per-slot condition chain is another bracket-kv token beside `src(...)`/`use(...)`). | `decision:condition subject — decoupled chain vs same-subject` | FW-26, FW-57, FW-56, FW-60, FW-43 | memory `deferred_features.md` (spitball, no design); wire → `.claude/plans/src-chain-encoding.md` |
| FW-28 | Composition-of-composers | Nest {{join}}/{{try}}/{{if}}. Runtime nesting trivial (composer callback resolves children from its options; GB sees only outer tag). `@name` reference NOT viable (GB stateless — verified). Only nested RESOLUTION works; AUTHORING-UI is the gating cost. Models: true-recursive vs one-level. | `decision:authoring-UI model` | FW-26, FW-29 | memory `deferred_features.md` (nesting tension) |
| FW-29 | Admin-built composite tag | `{{custom}}` + template selector. Counter to in-block nesting: build the over-complex tag in an admin UI, persist server-side (named template, `{{call}}`-store precedent), reference via `{{custom tpl:name}}`. Sidesteps flat-options serialization wall. May be the authoring SUBSTRATE for heterogeneous join/if/try via a `tpl:` option. | — | FW-28 (substrate) | memory `deferred_features.md` (counter-concept + substrate spitball, no design) |
| FW-30 | Block editor sidebar migration tool | | — | FW-31 | memory `deferred_features.md` |
| FW-31 | GB ↔ BWS tag cross-converter | | — | FW-30 | memory `deferred_features.md` |
| FW-32 | Primary-source + ref-hop parity | *(handle keeps "ref-hop" — a tracked row's identity; the body uses `refs` STEP per the 2026-08-05 vocabulary pass.)* A `refs` step starts only from a detected origin today; need pick-primary-then-step, incl. a `refs` step off an ID source, AND the fan-out preserved (not collapse-to-first). UX-open: likely a separate ref-step option per-`src`-value. Unifies with the ID source (FW-39). Carries the `{{table}}` relationship-producer seed (a relationship row-set = `src:ref` fan-out preserved). If a multi-hop path is ever encoded inline (vs. a per-`src` option), the step-separator decision now lives in FW-56 (the src-chain wire), NOT here — see it, don't re-litigate. Historical note (superseded): this row once leaned `/` (dodges the `+`=space urldecode trap; `+` signals set-union not an ordered step). **The 2026-07-29 sandbox frontrunner uses `+` as the step separator** — the `/`-vs-`+` call is REOPENED and provisional (the "urldecode trap" is a URL-layer concern, not GB's `parse_options()` which never urldecodes — `gb-constraints.md` §Option values NOT trimmed). Resolve in FW-56. | — | FW-10, FW-9, FW-56, `{{table}}` (relationship rows) | `.claude/plans/ref-hop-parity.md`; separator call → `.claude/plans/src-chain-encoding.md` §2026-07-29 |
| FW-33 | `term_` deprecation path | Subsumed by base + context-aware #19 + the ID source (FW-39); re-add registry-only after. NB `view_` does NOT follow — external plugin, may stay even when `src:view` lands. | `row:FW-9`, `code:ID source lands` | FW-8 | memory `project_term_deprecation_path.md` |
| FW-34 | Configurable default field keys per source × tag-type | | — | — | GH #29 (memory `project_default_field_keys.md`) |
| FW-35 | datetime_ all-day boolean field | Read `_piecal_is_allday` / ACF `true_false`, trim to date-only and/or show a note (each toggleable). | — | FW-3 | GH #41 |
| FW-39 | ID source | New source flavor: author identifies ONE specific entity, its id serialized into the token. **Encoding DECIDED via FW-56 DECISION 3 (2026-07-27): `<kind>,<ID>`** (`post,9999`/`term,34`/`user,7`) as the first step of a src chain — the kind on the wire is FORCED by the editor-UX static-computability floor (constraint 1: no live ID resolution in Patterns/Elements), and matches the engine `{kind,id}` shape 1:1. `id,9999`/bare `9999` rejected (kind not statically derivable). **Parser-recovery affordance (build-time, FW-56 grill 2026-07-27):** on `term,<arg>`, if `<arg>` is non-numeric it can't be a term ID → recover as a `terms` STEP (`<arg>` = taxonomy). Unambiguous (term-IDs numeric, taxonomy slugs non-numeric = disjoint value-spaces); guards the unique `term`/`terms` dropped-`s` mis-edit hazard. Confirm at build no taxonomy can have a purely-numeric slug. The author-supplied-entity-id pole of the source-binding model. Editor = pick-a-post/term. Home for the "specific-resource + site fallback" the #37 modifier filter left homeless; belongs as a try_ attempt (`try_allow_site_slot`), NOT a `try_term_` form. UX-open: ref-step decoupling (per-`src` ref option). | — | FW-32, FW-33, FW-9 | CONTEXT.md §Language "Source binding" (concept + two-axis model); no plan/issue yet |
| FW-40 | `datetime_single` `use` addition | `use` enum `modified` \| `key`; post source resolves the `post_date` analog bare (stripped default, `??` recovery — `published` is NOT a token, analog corollary); term/site have no native date → key required. Wire shape gated on the global-vs-scoped combine decision. Lowest-priority of the datetime cluster. | `decision:global-vs-scoped combine` | FW-41, FW-20, FW-3 | `.claude/plans/combined-option-controls.md` §Datetime application (single) |
| FW-41 | Datetime key pair-combine | Fold the time key into its partner date key, comma-appended: `key:date,time` (single), `startKey`/`endKey` each `date,time` (range). Six key options → three; halves FU-3 filter stacking; composite control = srcTermIn pattern instance #3; public-key rename → migration rows. Independent of the `use` fold (FW-40) and of global-vs-scoped. FW-2 (shipped 1.15.0) delivered the single normalizer (`bws_normalize_datetime_options`), so this lands as a one-site parse change there. | — | FW-14, FW-20, FW-40, FW-13 | `.claude/plans/combined-option-controls.md` §Lighter variant — keys-only pair-combine |
| FW-44 | join per-slot inner list `sep` (`{N}-sep`) | A list-mode join slot joins its own items with text's default `', '` (ADR 0003). **Blocker DISSOLVED (2026-07-23):** the prerequisite tag-level-assembly-`sep` rename shipped under FW-52 (→ `valueSep`, not the old `glue` guess — see `.claude/plans/join-sep-rename-handoff.md`), so a slot-1 bare `{N}-sep` no longer collides. Remaining: still an edge affordance; add only on evidence a per-slot inner separator is wanted. **Overlaps FW-61 (per-step `sep` on a fan-out chain, 2026-08-01)** — under the FW-57 fold a list slot's items come from its chain's terminal step, so FW-61's step-scoped `sep(…)` would deliver this affordance without a `{N}-sep` key at all (it rides inside the chain value, outside GB's flat key map). Decide them together; do not build `{N}-sep` without checking whether FW-61 subsumes it. | — | FW-43, FW-61 (overlaps) | `docs/adr/0003-join-per-slot-limit-not-sep.md`; `.claude/plans/join-sep-rename-handoff.md` |
| FW-45 | join dynamic slot count | Drop the fixed `BWS_JOIN_MAX_SLOTS` (10) for an add-slot editor control (arbitrary N). The constant is threaded through resolve loop / token scan / option emit so the cap raise itself is one change; the CONTROL is the blocker. **Control largely ANSWERED by the FW-57 slot REPEATER, SHIPPED 1.17.0 as `assets/js/slot-fold-control.js` (spike 2026-07-30/31, deleted at ship):** register-to-ceiling + render-a-slot-iff-it-holds-a-value, with "Add slot" writing an explicit seed value — no reveal chain at all (the `show_if` reveal-chain threading this row cited is DEAD under the fold; it was the pre-repeater model). Residual FW-45 scope is now (a) whether the ceiling stays finite (spike used 8; shipped join is 10) or the registration itself goes dynamic, and (b) applying the repeater to FLAT `{N}-src`/`{N}-use`/`{N}-key` join slots if the fold doesn't land first. **✅ Pass-5 re-scope DONE (2026-07-31, corrected 2026-08-01): residual is (a) the ceiling question, (b) applying the repeater to FLAT join slots if the fold doesn't land first, and (c) REORDER.** Reorder STAYS in FW-45 — assigned to it by the detail home in three places (`.claude/plans/src-chain-encoding.md` L831 "append-a-step `bws-chain` (FW-45's target): unbounded + drag-reorder", L843 "uncapped + reorder wait for FW-45", L2132 materialization stated for "removing or reordering") and NOT discharged by the FW-57 repeater, which builds add + remove only. *(An interim Pass-5 note excluded reorder on the grounds it "was never in this row" — wrong: the row INDEXES, the plan is the detail HOME, so scope was read off the wrong artifact.)* The cap-focused phrasing above does not narrow the row to (a): `BWS_JOIN_MAX_SLOTS` bounds slot COUNT not ORDER (`join-helpers.php:61-68`), so uncapped-N and slot-order are independent axes FW-45 owns both of. **Sharpening worth carrying into the build:** reorder is strictly worse than removal, not equivalent — removal touches only the immediate successor (single fixup, `compaction-probe.js` 18 cases), whereas reorder moves a slot PAST others so every intervening `same` re-points, and a slot dragged into position 1 hits the where-`same`-is-illegal case from the other direction. A materialization CASCADE with no coverage (the probe is a removal harness). This is exactly why L831 holds that v1 must not take the new control AND the `same`-rewrite burden at once. **Repeater is also the control FW-60 would need** (absorbing `try_` into base = an add-slot control on a base tag), so if that lands the repeater serves three containers, not two — no new control work, but the ceiling question (a) gains a case where the floor is 1 rather than 2. | `code:custom editor-control work (docs/editor-controls.md reserved owner)` | FW-24, FW-57, FW-60 | `.claude/plans/archive/combine-text.md` §Slot count; repeater → `.claude/plans/src-chain-encoding.md` §Slot repeater |
| FW-46 | Name-format preset over join | Canned "Full name" preset pre-filling `mode:template` + the 7-part format + slot keys — pure config sugar, no new resolve path. LEANED direction vs a dedicated `{{name}}` tag (parked: "name" collides with term-name/post-name/repeater-subfield semantics; revisit only on persistent mis-wiring AND a collision-free name, e.g. `{{person}}`). | — | FW-29 (preset-authoring substrate) | `.claude/plans/archive/combine-text.md` §Open/deferred (both ends recorded) |
| FW-48 | `src:author` — the current post's author as a user source | New source flavor reaching a USER source by post→author hop: `{{title src:author}}` → the post author's display name, `{{content src:author}}` → their bio, on a singular post (NOT an author archive — that's the ambient FW-9 author kind). Mechanically small — the factory resolves `{ kind:'user', id:(int) get_post_field('post_author', $post_id) }` and the existing user analog readers (`bws_base_user_analog_read`, shipped 1.15.0) do the rest; it's a new REACH to the user kind the way `src:ref` reaches a post. Two scope corrections (rescan 2026-07-20): (1) the traversal layer is already user-aware (ambient resolve, step engine, ref-step `user_{id}` read) but seam L2 `bws_read_resolved_source()` has NO `user` case — the `use:key` user-meta read dies silently until that small arm lands (in-scope here, hence the gate); (2) CONSTRAINT: user-analog reads currently live only in the title/content CALLBACKS, not in `bws_base_text_resolve_value()` — any user expansion this row triggers MUST land in resolve-value (its ABSORB INVARIANT), else join slots + try_text never inherit it. Opens the same permalink/image analog questions as FW-47 (here permalink is NON-circular — the author's archive from a post that isn't it — which is exactly the non-ambient user source FW-47's permalink gate was waiting on). Also the natural pairing point for a `use:key` user-meta read. UX: a plain `src` dropdown value, no extra control. **Build-grouped with FW-49 (2026-07-21):** the gate below is one `case 'user':` in the SAME switch FW-49 already edits (`bws_read_resolved_source`, field-helpers.php:398-426 — handles site/term/meta_row/post only); everything else is already user-aware (traversal typedef :20, step-engine input kinds :120, ambient resolve :301-302, ref-step user-meta read :533, `bws_resolve_link_url` link-helpers.php:90). And constraint (2) is an FW-49 bug in waiting — converging text's resolve onto a seam with no user case ships a hole in the ABSORB seam (join-helpers.php:90-96, try_text), then re-opens the same function to fill it. Landing together gives join/try inheritance for free AND proves the per-value `{value,id,kind}` payload's kind-extensibility claim instead of asserting it. **Scope guard:** seam user arm + factory post→author hop + resolve-value user arm ONLY — the permalink/image analogs this opens stay FW-47's decision. **Seam halves SHIPPED 1.16.0 (FW-49 build):** `case 'user':` in `bws_read_resolved_source` (plain gated `get_user_meta`; unreachable until the hop lands) + the resolve-value user arm (`bws_base_text_resolve_value` ambient-user branch + `bws_base_user_analog_read` text case — {{text}}/{{join}} slots now resolve on author archives; try_ slots still don't, see FW-43). **Remaining = the factory post→author hop** that makes `src:author` user-facing. | — | FW-47, FW-32, FW-39, FW-9, FW-49 | seam halves' record `.claude/plans/archive/traversal-convergence-fw49.md`; readers exist (`bws_base_user_analog_read`), factory hop + seam user arm are the new code |
| FW-54 | `src:query` — cross-tag post-query base source | A base source that runs a `WP_Query` from author filters (post type, tax, meta, orderby, limit) and roots the tag on the RESULT — sits beside current/ref/site at L1, ignores ambient (parallel to `src:site`). Cross-tag by nature; `{{table}}`/`{{list}}` exploit its plural post set best. Split OUT of the `{{table}}` alternate-row-source work (FW-53) because it is NOT parallel to relationship rows (an L2 field-read that keeps the base single) — query is a new **fanning** base source at L1, with three structural costs beyond the query-filter UI: (1) upstream blast radius — every scalar tag consuming `src:query` needs a collapse rule (the layer assumes ONE base entity today); (2) editor preview must run a live `WP_Query` (query ignores the ambient id that I11 threads); (3) dynamic field-discovery scope (target post type comes from the author's still-building filter, not a static ACF restriction). Shares FW-53's `meta_row`-loop de-hardcode + post-type discovery generalization. CONCEPT, not fleshed. | — | FW-53, FW-32, FW-13 | `.claude/plans/query-source.md` |
| FW-59 | Bracket free-form values on BASE tags | Base tags today emit free-form option values RAW (`format:g:i A`, `fallback:N/A`, join `valueSep:; `), relying only on `\:`/`\|` escaping (`bws-format-input` does the `format` escape; others may not). The FW-56/57 slot work established a cleaner uniform rule the base tags would benefit from: **wrap free-form values in a bracket** (`format[g:i A]`, `fallback[N/A]`) so the value is VISUALLY + structurally isolated from the option separators, not just escape-dependent. Brackets are INERT to GB (`( ) [ ]` serialization-safe, `gb-constraints.md` §Separator-safe) so this is purely a serializer/parser change on OUR side, no GB-wire risk; a balance-aware sub-parser handles balanced inner brackets, `\:`/`\|` still escapes the two GB-structural chars, `{ }` remain the only hard-unsafe (rare in these values). Surfaced 2026-07-29 eyeballing the `tools/preview/tag-string-preview.html` escape-hazard scenarios — `format[g\:i A]`, `label[Price\: USD]`, and the ⚠ unbalanced-bracket flag showed the bracket+escape discipline reads cleanly and would regularize base-tag free-form emission the same way. Scope-open: which option keys count as free-form (format/fallback/valueSep/rangeSep/timeSep/sep/label — the `RESPELL_FREEFORM` set is the seed); whether it's a migration (existing raw values re-serialize) or read-tolerant (accept both bracketed + bare); interaction with `bws-format-input`'s existing escape control. Should land WITH or BEFORE FW-56/57 so base + slot free-form emission share one rule (avoid a base/slot spelling split). | `decision:migration-vs-read-tolerant` | FW-56, FW-57 | `.claude/plans/src-chain-encoding.md` (escape-hazard finding); `gb-constraints.md` §Tag string escape syntax + §Separator-safe |
| FW-60 | Absorb `try_` into base tags via an add-slot control (one-way fold) | Make "try another source" reachable as a CONTROL on a base tag rather than a separate tag family: `{{text}}` grows slot 2, and N>1 behaves as today's `{{try_text}}`. **Forced by a GB constraint, not chosen for elegance** — switching tag type in the modal discards all options (`gb-constraints.md` §Switching tag type), so a base→`try_` conversion that preserves configuration CANNOT be a tag-type switch; it can only happen inside one tag's option set. Today the author's only route is hand-editing the tag string (retype the name, keep the options), which works because base and `try_` spell options identically — **the FW-57 fold ends that** (`key:x` → `{N}:` prefixed), so post-fold there is no base→try_ upgrade path at all unless this lands. That regression is the actual driver (user-reported workflow, 2026-08-01). **Encoding falls out cleanly: ONE-WAY fold.** Base tags serialize unprefixed until the tag acquires slot-scoped state, then fold once and stay folded; parser accepts bare-as-slot-1 permanently (a compat tail owed to existing content regardless). One-way drops the two costs that sank the bidirectional auto-switch considered first (2026-08-01): no round-trip, so no destructive collapse of a removed slot's config, and no history-dependent wire shape (which ADR 0004 hand-editability forbids); the forward migration alone has precedent in `bws_migrate_image_as_size_fold`. **Fold TRIGGER is probably not slot count** — if FW-27 per-slot conditions land, slot 1 can carry slot-scoped state (a condition) with no slot 2, so the trigger is "tag needs slot-scoped state", not "N>1". Cleaner rule; settle with FW-27. Rejected alternative: S1-always-flat-except-where-col-1-differs (`{{table}}`) — makes the slot-1 prefix a per-tag fact rather than a grammar rule, so every slot consumer (preview builder, reveal, FW-57 fold, FW-56 chain steps, FW-52 KEY_MAP, FW-24) must branch on tag identity, and the decoder needs the tag name in scope before it can tokenize options (today options tokenize tag-independently — a real narrowing of FW-56's static-computability floor). **Consequences if it lands:** `try_` becomes registry-only entries (renderers dissolve into base, rows kept for existing content — the 1.14.0 no-migration-target precedent, memory `project_deprecated_tags_no_migration_path`); same dissolution shape as FW-33 `term_`. **User assessment 2026-08-01: PREMATURE** — recorded as a possibility, not a plan. **Open before it could start:** (1) is `try_` really "base + N slots", or does it carry structural difference? (user's read: absorption should be clean — `try_` absorbs and iterates base tags — but `generate_base_try_tags()` holds the inline selecting-shaped resolve + `show_if_any` reveal that FW-43 targets, so verify against that extraction, not the tag surface); (2) the add-slot control is the same custom-control work FW-45's repeater spike largely answered; (3) labeling — "Add fallback" REJECTED (collides with the shipped `fallback` static-text/image option, same collision Pass 5 hit for `try_`'s slot noun), leaving "Add alternative" / "Try another source" as the live candidates, and this row and FW-57's held `try_` noun should be named together. | `decision:premature — absorb try_ into base at all` | FW-57, FW-45, FW-27, FW-43, FW-24, FW-33 | this row (no plan file yet); constraint → `gb-constraints.md` §Switching tag type; label collision → `.claude/plans/src-chain-encoding.md` §Pass 5 |
| FW-61 | Per-step `sep` on a fanning chain | Today ONE tag-level `sep` joins a whole fan-out, so a chain with two fanning steps has a single joiner for both and cannot render `A; B / C`. Split it per fanning STEP: an ordinary bracket-kv token beside `limit`, obeying the same depth alternation (`sep(; )` at depth 0, `sep[; ]` inside a slot wrapper) and the same free-form escaping (`\:`/`\|`) as every other free-form value. DEFERRED 2026-08-01 (user) — modelled and rendered, not adopted. It was built in the preview tool to give a step a SECOND kv token, which is what let the slash-`limit` candidate be judged and rejected; that job is done. Wire shape and escape behaviour are settled by the same render (fixtures cover a bare glyph, values carrying the step/opt/kv/GB-reserved chars, and a 3-hop chain with a different sep per step — the case one tag-level `sep` cannot express at all, which is the argument FOR it). What is NOT settled: whether the authoring surface is worth it, and how it interacts with the tag-level `sep` it would not replace. Own preview toggle (`S.perStepSep`, default off). **Overlaps FW-44 (join per-slot inner list `sep`) — read them together.** Under the FW-57 fold a join slot's list comes from its src chain's LAST step, so a per-step `sep` on that step IS FW-44's per-slot inner sep; FW-61 additionally reaches INTERMEDIATE steps, which `{N}-sep` cannot express. FW-61 also carries a wire argument neither has: a step-scoped `sep(…)` lives INSIDE the chain value, so it never enters GB's flat key map and cannot collide with a tag-level key — which is the collision ADR 0003 rejected `{N}-sep` over (already dissolved separately by the FW-52 `sep`→`valueSep` rename). ADR 0003's OTHER reason still binds both rows and is why this is deferred rather than open: edge affordance, no evidence it is wanted. | **fan-out STRUCTURE preservation (FW-32)** — re-blocked 2026-08-05 | FW-44 (overlaps), FW-56, FW-57, FW-55 (unsafe-char warn UI), FW-20, FW-53 | `.claude/plans/src-chain-encoding.md` §SLASH candidate (per-step `sep` subsection); the structural block is `.claude/plans/per-hop-limit.md` §Deferred UX — a sep joins printed VALUES while a step produces SOURCES, so a per-step sep only means something if output is a TREE, and `bws_run_traversal` explicitly flattens (`$next[] = $produced`). Not a control-shape question |
| FW-62 | Move the fold control's remaining authored LABELS onto the option definition | `assets/js/slot-fold-control.js` still types four strings of its own: `__( 'Source' )` (×3, incl. the `Source path` variant a multi-hop chain switches to) and `__( 'Taxonomy' )` on the `terms` step arg. Everything else the repeater renders — enums, nouns, the read label, the per-slot flat axis surface, and (1.17.0) the step slug map — arrives DERIVED on the PHP option definition, which is the standing rule: re-typing a string at a consumer is how image's `Return type:` / `Return image as:` drifted, and how a hand-kept axis list deleted a `try_text`'s tag-level `limit`. These four are the residue, found by code review 2026-08-04. **Not urgent, and the reason is worth stating:** a label has no wire consequence, so a drifted one misleads an author rather than corrupting a tag — unlike the axis list, whose divergence rewrote stored options. **Do it WITH the surface that is already going to touch them:** `bws_build_fold_slot_options()` gains label keys (a `hopRows` entry could carry its own arg label, which is also what a second relationship hop needs), the control reads them, and the derive gains a case in `slot-options-build-test.php`. Two callers pass them today (join + the try_ registration loop), so the cost is one config addition, not a redesign. | — | FW-20 (the combine that re-touches this control), FW-32 (a second `refs` step needs a per-step arg label anyway), FW-45, FW-53 | this row (no plan file); migrates to `docs/editor-controls.md` when that doc is created — CLAUDE.md already reserves it as the owner for the `bws-*` control pattern |

## Closed / Retired

Append-only ledger of closed, shipped, or cut work — both `FW-N` rows deleted from the live
table above AND pre-tracker refactors (the legacy `C#` / GitHub-`#issue` handles from the old
`project_open_refactors` memory, folded in here so there is ONE closed record). IDs are
**permanent** — a retired `FW-N` is never reused or reassigned. This ledger is the only record
of the FW high-water mark once shipped rows are deleted; **"next unused id" = (max `FW-N` here ∪
max `FW-N` in the live table) + 1**. One line per item: outcome + where it landed. Not a tracker
(no blockers/interactions) — just the closed record + a pointer to detail.

| ID | Item | Outcome | Landed / detail home |
|---|---|---|---|
| FW-1 | Deprecated tag removal | Shipped 1.14.0 | CHANGELOG 1.14.0; `deprecated-tags.php` PHPDoc; memory `project_deprecated_tags_no_migration_path` |
| FW-2 | Datetime option-key cleanup | Shipped 1.15.0 (datetime pass, #48): single normalizer `bws_normalize_datetime_options()` — the ONLY parse point; mappers kept as portal-system compat wrappers; FW-41 lands as a one-site parse change | CHANGELOG 1.15.0; normalizer PHPDoc (datetime-tags.php); `tools/test/datetime-format-test.php` N-group |
| FW-4 | `src:site` slot for the remaining `try_` tags | Shipped 1.15.0 (2026-07-20): the "content/image/analog seam arms" gate was phantom (rescan) — pure wiring. `try_site_fn` descriptor leg in the registry site arm (falls back to `$cf(0,…)` — email/phone byte-identical); five thin closures over `bws_site_resolve_value` (shipped v1.9.0); single-result site link-wrap `('site', 1)` for I6/C9 parity | CHANGELOG 1.15.0; registry PHPDoc (`try_site_fn`); `src-site-test-matrix.md` R7 + matrix-post-meta "Site R7" blocks |
| FW-5 | Collapse the `try_core_fn`/`try_term_fn` fork | Merged into FW-43 (2026-07-20 rescan/grill): the fold extraction's slot handler IS the collapse target; its "seam expansion proves kind-dispatch shape" gate was already satisfied by `bws_read_resolved_source` (shipped 1.14.0) | FW-43 row (absorbed as internal step) |
| FW-6 | Datetime list mode | Shipped 1.15.0 (GH #30, #48): `limit`/`sep` on both datetime tags, text/title V14 parity, src:ref plural fan-out; collect loop FW-3-shaped (ACF-object-id items) | CHANGELOG 1.15.0; `tag-reference.md` §List mode; `tools/test/datetime-test-matrix.md` D4 |
| FW-11 | Gate wrap-capable base tags on img/picture | **Cut 2026-07-21 — inert, no code shipped.** Premise doesn't hold: no editor-reachable GB block presents `tagName` `img`/`picture` to the picker's compare, so such a gate can never fire. Container's tagName enum excludes both; media serializes `"img"` but the picker reads an unpopulated prop and falls back to `''`. Implemented across 9 sites, then reverted. Byproduct: corrected the mechanism in `gb-constraints.md` + `bws_tag_blocked_on_media_block()` PHPDoc (both had wrongly said media "never serializes" tagName), and established that only the `a`/`button` half of email/phone's existing gate does real work | GH #31 (closed inert); `gb-constraints.md` §visibility blind spot |
| FW-12 | Custom time format on two-ended `as:time` range | Shipped 1.15.0 (GH #25, #48): per-side format via the single-ended resolver chain; consolidation gated to 12-hour formats | CHANGELOG 1.15.0; `bws_format_time_range()` PHPDoc; matrix D3 |
| FW-22 | `{{join}}` tag | Shipped 1.15.0 (standalone combining tag, 10 text slots, separator + template modes, %N wire tokens). Spawned FW-43/44/45/46. | CHANGELOG 1.15.0; `tag-reference.md` §join; `tools/test/join-test-matrix.md` + `join-template-test.php`; plan archived `.claude/plans/archive/combine-text.md` |
| FW-36 | Deprecated vs Removed settings split (tags AND options) | Shipped 1.14.0 (absorbed FW-37) | CHANGELOG 1.14.0; `MigrationRegistry::is_entry_live()` PHPDoc + CONTEXT.md I10; FW-38 is the principled successor |
| FW-37 | Settings-split sub-item | Merged into FW-36 before ship | see FW-36 |
| FW-49 | Base text/title list collection convergence + seam return-shape (link identity) | Shipped 1.16.0 (FW-49 build, one commit per step b873a9c→): shared L3 combining fold `bws_collect_value_list()` (field-helpers.php) replaces the four text/title list loops AND datetime's local fold (deleted); per-value link identity `{kind,id}\|null` + single-result link gate (CONTEXT.md I12); the frozen `string[]` seam gains only an optional `$links` out-param (`bws_source_link_identity()`); GH #51 suppression now structural (lives in the fold). FW-48 seam halves + FW-3(b) payload half shipped in-build (see those rows). Byte-parity: 45-case render-tag diff clean vs pre-build baseline | CHANGELOG 1.16.0; CONTEXT.md I12; `bws_collect_value_list()` PHPDoc; tag-reference §Output shape list-mode note; `.claude/plans/archive/traversal-convergence-fw49.md` (full record: rescan, build composition, grilled design) |
| FW-50 | Remove the `fallback_text` active read path | **Work complete 2026-07-21; ships in 1.16.0 (unreleased at time of writing — FW-49's `ship:1.16.0` gate clears on release).** Cores read `fallback` directly; both reverse-mappers deleted (`bws_base_map_options()` removed entirely — its sole job was the backwards rename, leaving an identity function); `{{join}}`'s registration corrected (it shipped 1.15.0 on the legacy key — clean rename, no migration entry: one release old, option unused). Scope grew on contact with the code: six option builders registering the key turned out to have zero call sites (deleted), and `bws_post_term_extraction_options()` was published-but-doc-drifted (kept, key renamed to match its documented contract). Also carried GH #51's fix, since the per-item fallback contract had to be uniform before FW-49 converges these callbacks | CHANGELOG 1.16.0; rename row `docs/deprecated-tags-options.md` (Implemented); GH #51; FW-49's gate cleared |
| FW-52 | Serialization-order decoupling (reorder normalizer + registration-unwind) | Shipped 1.16.0 (2026-07-23, branch `docs/fw-52-serialization-order`): canonical CONTROL order (`source → format → link → fallback`) via registration standardization + canonical SERIALIZE order (`format → source → link → fallback`) via a per-tag `tagSpecificControls` normalizer (invisible `setState` reorder, `N-`-slot-contiguous); as+size composite co-shipped (C2 coexistence). Datetime time-key `show_if` stripped. Excluded: `link`-cluster unification (stays deferred to FW-20). | CHANGELOG 1.16.0 (Highlights + Changed); `serialization-order-normalizer.js` + `bws_serialization_order_sort()` PHPDoc; `tools/test/serialization-order-test.php` + `as-size-fold-test.php`; `.claude/plans/combined-option-controls.md` §Grill outcomes 2 |
| #21 | Editor preview: resolve-then-label | Closed 2026-05-19 (commit 9f4fa96), shipped v1.6.2 | Resolve-then-label on all base/modifier/try/datetime callbacks; CHANGELOG v1.6.2 |
| #26 | Derive try_ slot option DEFS from base builders | Closed 2026-06-26 | `bws_build_slot_traversal_options`; option-DEFINITION derivation only (NOT the resolve loop / `show_if_any`) — see memory `project_open_refactors` residual note |
| C1 (#2) | Consolidate field extraction logic | Closed 2026-05-01, shipped v1.6.0 | `bws_read_field()`/`bws_read_term_field()` in `content-helpers.php` (route through `GenerateBlocks_Meta_Handler`); CHANGELOG v1.6.0 |
| C4 (#3) | Extract post-content rendering pipeline | Closed 2026-06-01, shipped v1.8.0 | `ContentProcessor` (`includes/classes/content/class-content-processor.php`); `bws_render_block_content()`; CHANGELOG v1.8.0 + `docs/post-content-processing-reference.md` |
| Traversal pipeline Phase 1 | Ambient-context source factory + term-kind base tags | Shipped 1.14.0 | CHANGELOG 1.14.0; `.claude/plans/traversal-pipeline.md` (later phases = FW-3/4/5/7/8) |

## Maintenance

- New non-bug idea → add a row with the next unused `FW-N` — **(highest id in the live table
  ∪ highest id in Retired IDs) + 1**; never reuse a retired id + put detail in its home (plan
  file / issue / memory). Don't let an item exist *only* in a hidden file with no tracker row.
- **Build starts** (branch + committed work toward a named version) → **move the row to `### In
  flight`** and add a **Target** cell (the landing version). The row keeps its `FW-N`, its
  description, and all pointer/gate cells — only its section changes. Do NOT start recording
  phase/commit/remaining-tasks in the row; that state stays in the branch / plan / unreleased
  CHANGELOG (the FW-52 staleness rule). The section IS the lifecycle signal.
- Item ships (or is cut/merged) → delete its row (from wherever it sits, In-flight included) once
  CHANGELOG records it, **and append a line to Retired IDs** (id + outcome + where it landed). Its
  `FW-N` retires — do not reassign it. Update any surviving row that referenced it (`row:FW-N` →
  satisfied gate can be dropped; `Interacts with` id removed).
- Blocker clears or a new interaction surfaces → update the cell; that's the point
  of those columns. Certainty (concept → planned) is read from the detail home,
  not tracked here. **Lifecycle** (future → in-flight → shipped) is read from the
  section, not a cell.
