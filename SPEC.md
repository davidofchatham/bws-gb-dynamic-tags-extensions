# SPEC — No active spec. See CHANGELOG, docs/tag-reference.md, Issues.

> Prior spec (deprecated tag registration + callback removal, 1.14.0) post-ship-migrated 2026-07-07. Invariants → PHPDoc on `bws_register_deprecated_tags()`, `bws_register_v1_deprecated_tag_wrappers()`, `bws_register_early_deprecated_tag_migrations()` in `includes/tags/deprecated-tags.php`. Schema/current-state → `docs/tag-reference.md`. Shipped detail → CHANGELOG 1.14.0. Bugs B1 (25 no-migration-path tags dropped) and B2 (`key` option alias not migrated) found + fixed in-branch before ship, no GitHub issues filed. Follow-on (Keep/Suppress/Disable now dead code) tracked `docs/future-work.md` FW-36 — new SPEC to follow on this same branch before merge to main.
