# SPEC — No active spec.

FW-36 (deprecated/removed settings split + scan-hide) shipped in 1.14.0. Invariants live in
CONTEXT.md I10 (+ `MigrationRegistry::is_entry_live()` / `render_page()` PHPDoc); schemas in
docs/tag-reference.md; feature summary in CHANGELOG [1.14.0]. Future direction: docs/future-work.md FW-38.

See CHANGELOG, docs/tag-reference.md, Issues.
